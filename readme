这是一个根据被试的HRV数据来判断她们的情绪状态的算法：

SVM文件夹中，用SVM和MLP来对从原始RR间期数据中提取的6个特征进行训练
1DCNN两个文件夹中，分别对LJY和ZBY进行训练，其中LJY的数据是分布比较均匀的。

受限于数据质量，模型暂时无法达到比较高的准确率。
但我们期待，未来随着数据质量的提高，更高采样率的传感器所采集到的更加准确的情绪标签，以及我们拥有的大量的40s时长的数据，我们相信模型的准确率会有显著提升。